{% macro render(id)%}
    <div class="qm-card-nav">
        <div class="qm-card-nav__left">
            <button class="qm-action-btn qm-action-btn--only-icon">Back</button>
        </div>
        <div class="qm-card-nav__middle">
            <h2 {% if id %}id="{{id}}"{% endif %} class="qm-card-nav__title">Add delivered services</h2>
        </div>
    </div>
{% endmacro %}