{% macro render(id, type)%}
    <button 
        {% if id %} id="{{id}}" {% endif %}
        class="qm-btn {{type}}">
        <span class="qm-btn__text" title="Send">Send</span>
    </button>
{% endmacro %}